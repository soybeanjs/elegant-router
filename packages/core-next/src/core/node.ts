import type { ResolvedGlob } from '../types';
import { AutoRouterNode } from '../types';

export function resolveNode(glob: ResolvedGlob) {}

function resolveGroupNode(glob: ResolvedGlob) {}

function resolveParamNode(glob: ResolvedGlob) {}

function resolveNormalNode(glob: ResolvedGlob) {}
